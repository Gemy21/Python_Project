# دليل الاستخدام السريع - نظام مزامنة البيانات

## ✅ تم إضافة نظام المزامنة بنجاح!

### 📍 ما الذي تم إضافته؟

1. **ملف `data_sync.py`** - النظام الأساسي للتصدير والاستيراد
2. **زر "مزامنة البيانات"** في الشاشة الرئيسية للبرنامج
3. **واجهة رسومية** سهلة الاستخدام
4. **سكريبت النسخ الاحتياطي التلقائي** (`auto_backup.py`)
5. **ملف إعداد الجدولة** (`setup_auto_backup.bat`)

---

## 🚀 البدء السريع

### الطريقة الأولى: من خلال البرنامج (الأسهل)

1. **شغل البرنامج**: `python main.py`
2. **اضغط على زر "مزامنة البيانات"** في الشاشة الرئيسية
3. **للتصدير**: اضغط "تصدير البيانات الآن"
4. **للاستيراد**: اضغط "استيراد من ملف" واختر الملف

### الطريقة الثانية: من سطر الأوامر

```bash
# تصدير البيانات
python -c "from data_sync import quick_export; quick_export()"

# استيراد البيانات
python -c "from data_sync import quick_import; quick_import('data_exports/backup_2025-12-02.json')"
```

---

## 📤 كيف تنقل البيانات بين جهازين؟

### على الجهاز الأول (المصدر):
```
1. افتح البرنامج
2. اضغط "مزامنة البيانات"
3. اضغط "تصدير البيانات الآن"
4. ستجد الملف في مجلد: data_exports
```

### نقل الملف:
```
- انسخ الملف من مجلد data_exports
- ضعه على USB أو أرسله بالبريد
- انقله إلى الجهاز الثاني
```

### على الجهاز الثاني (الهدف):
```
1. ضع الملف في مجلد data_exports
2. افتح البرنامج
3. اضغط "مزامنة البيانات"
4. اختر "تحديث (إضافة وتحديث)"
5. اضغط "استيراد من ملف"
6. اختر الملف المنقول
7. أكد الاستيراد
```

---

## ⚙️ طرق الدمج - أيهما تختار؟

| الطريقة | متى تستخدمها | الأمان |
|---------|--------------|--------|
| **تحديث** | المزامنة اليومية العادية | ✅ آمن |
| **استبدال** | بداية جديدة أو استعادة كاملة | ⚠️ خطر |
| **تخطي** | دمج بيانات من مصدرين مختلفين | ✅ آمن |

**💡 نصيحة**: استخدم "تحديث" دائماً للمزامنة اليومية!

---

## 🤖 النسخ الاحتياطي التلقائي (اختياري)

### الطريقة السهلة:
1. شغل ملف `setup_auto_backup.bat`
2. اتبع التعليمات لإعداد المهمة في ويندوز
3. سيتم النسخ الاحتياطي تلقائياً كل يوم!

### الطريقة اليدوية:
```bash
# تشغيل النسخ الاحتياطي مرة واحدة
python auto_backup.py

# تشغيل مع انتظار (لرؤية النتائج)
python auto_backup.py --wait
```

---

## 📊 ما الذي يتم تصديره؟

يتم تصدير **جميع البيانات** من:
- ✅ حسابات البائعين
- ✅ معاملات البائعين
- ✅ حسابات العملاء
- ✅ العدة
- ✅ الوجبات والأصناف
- ✅ ترحيل الزراعة
- ✅ المنصرفات
- ✅ فواتير العملاء

---

## 🔍 استكشاف الأخطاء

### المشكلة: "لا توجد نسخ احتياطية"
**الحل**: صدّر البيانات أولاً باستخدام زر "تصدير البيانات الآن"

### المشكلة: "خطأ في الاستيراد"
**الحل**: 
- تأكد من أن الملف بصيغة JSON
- تأكد من أن الملف تم تصديره من نفس البرنامج

### المشكلة: "بيانات مكررة"
**الحل**: استخدم طريقة "تحديث" بدلاً من "تخطي"

---

## 📁 هيكل الملفات

```
Python_Project/
├── data_sync.py              # النظام الأساسي
├── auto_backup.py            # النسخ الاحتياطي التلقائي
├── setup_auto_backup.bat     # إعداد الجدولة
├── DATA_SYNC_README.md       # الدليل الكامل
├── QUICK_START.md            # هذا الملف
└── data_exports/             # مجلد النسخ الاحتياطية
    ├── backup_2025-12-02.json
    └── daily_backup_2025-12-02.json
```

---

## ⚠️ تحذيرات مهمة

1. **احتفظ بنسخة احتياطية** قبل استخدام طريقة "استبدال"
2. **لا تشارك ملفات النسخ** مع أشخاص غير موثوقين (تحتوي على بياناتك)
3. **اختبر الاستيراد** على قاعدة بيانات تجريبية أولاً

---

## 💡 نصائح للاستخدام اليومي

### روتين يومي مُوصى به:

**نهاية اليوم (الجهاز الرئيسي):**
```
1. اضغط "مزامنة البيانات"
2. اضغط "تصدير البيانات الآن"
3. انسخ الملف إلى USB
```

**بداية اليوم (الجهاز الثانوي):**
```
1. ضع ملف USB في الجهاز
2. انسخ الملف إلى data_exports
3. افتح البرنامج
4. اضغط "مزامنة البيانات"
5. اضغط "استيراد من ملف"
6. اختر "تحديث"
7. اختر الملف وأكد
```

---

## 📞 هل تحتاج مساعدة؟

راجع الملف `DATA_SYNC_README.md` للحصول على:
- شرح تفصيلي لكل ميزة
- أمثلة برمجية متقدمة
- حلول للمشاكل الشائعة
- نصائح للأمان والنسخ الاحتياطي

---

**✅ الآن لديك نظام مزامنة كامل بين نسختين من البرنامج!**

استمتع بالمزامنة السهلة والآمنة! 🎉
